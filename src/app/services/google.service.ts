import { Injectable } from "@angular/core";
import * as googleDrive from '@googleapis/drive';

@Injectable({
  providedIn: 'root'
})
export class GoogleService {
  // constructor(private googleDriveService: Google) {}
}
